from django.db import models

# Create your models here.
class sites_regions (models.Model):
    pass
    class Meta:
        app_label = 'Site regions'
        verbose_name = 'Site regions'
        verbose_name_plural = 'Site regions'